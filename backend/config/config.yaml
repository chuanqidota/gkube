server:
  ip: 0.0.0.0
  port: 8080
database:
  user: root
  # 本地开发直填;生产环境请用独立 config 或环境变量覆盖,勿提交真实口令
  password: jinang123
  host: 127.0.0.1
  port: 3306
  name: gkube
elasticSearch:
  enable: false
  url: http://localhost:9200
  username: ""
  password: ""
audit:
  record_audit: "audit"
s3:
  endpoint: ""
  accessKeyID: ""
  secretAccessKey: ""
  useSSL: false
  bucket: ""
security:
  # JWT 签名密钥(必填,生产环境通过环境变量 JWT_SECRET 覆盖)
  jwt_secret: "gkube-dev-jwt-secret-change-in-production"
  # AES-256-GCM 加密密钥,必须为 32 字节(必填,生产环境通过 AES_KEY 环境变量覆盖)
  # 生成新密钥: openssl rand -hex 16
  aes_key: "c59fcf701939f705496c48f2b21e72e0"
  # CORS 允许来源白名单,留空允许所有来源(生产环境请显式配置)
  cors_origins: []
  # 管理员用户名白名单
  admin_users:
    - admin
